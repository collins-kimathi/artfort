# Logo Setup

Place your final uploaded logo image here as:

- `public/logo.png`

The app is already configured to try `logo.png` first for the header and loading skeleton, and falls back to `logo.svg` automatically if `logo.png` is missing.
